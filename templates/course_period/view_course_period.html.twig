{% extends 'base.html.twig' %}

{% block title %}Course Period List
{% endblock %}

{% block body %}
	<div class="flex items-center w-full h-full">
		{% include "Components/sidebar.html.twig" %}
		<div class="h-full mt-32 mx-24">

			{% for message in app.flashes('error') %}
				<div class="alert alert-danger">{{ message }}</div>
			{% endfor %}
			<h1 class="text-blue-950 text-3xl font-medium">Promotion
				{{ coursePeriod.schoolYear.name }}</h1>
			{{ include('admin/form/form_course_period.html.twig', { 'form': form }) }}
		</br>
		<div class="flex flex-row">
			<a href="{{ path('app_view_schoolyear', {'id': coursePeriod.schoolYear.id}) }}" class="px-4 py-2.5 text-black bg-slate-200 font-medium text-base rounded-xl mr-3">Retour à la liste</a>
			{{  include('course_period/delete_course_period.html.twig') }}
		</div>
	</div>
</div>{% endblock %}
